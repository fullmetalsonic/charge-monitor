# 결정 및 변경 기록

> English: [Decision and change log](en/03_decision_and_change_log.md)

이 파일은 프로젝트의 요구사항, 기술 판단, 검증 결과를 날짜순으로 **누적**한다. 기존 기록은 삭제하거나 덮어쓰지 않고, 수정 이유와 함께 새 항목을 맨 아래에 추가한다.

## 기록 형식

```md
## YYYY-MM-DD - 제목

- 구분: 요구사항 | 결정 | 검증 | 변경 | 보류
- 내용:
- 근거 또는 확인 방법:
- 영향 파일:
- 상태: 확정 | 검증 필요 | 보류
```

## 2026-08-18 - 기획 단계 시작

- 구분: 요구사항
- 내용: Galaxy Z Fold8에서 충전기 연결 시 자동으로 충전 전력 추정값을 상태바 알림에 표시하고, 기기 설정이 허용하는 경우 AOD 알림에도 보이게 하는 앱을 만든다.
- 근거 또는 확인 방법: 사용자 요구사항. 실제 AOD 표시는 Fold8 실기기 설정과 함께 검증한다.
- 영향 파일: `docs/01_기획서.md`, `docs/02_구조_및_파일분배.md`
- 상태: 확정

## 2026-08-18 - AOD 구현 경계

- 구분: 결정
- 내용: 일반 앱이 삼성 순정 AOD 레이아웃에 직접 W 텍스트를 삽입하는 기능은 범위에서 제외한다. 일반 Android 알림의 AOD 노출만 지원 대상으로 한다.
- 근거 또는 확인 방법: One UI의 AOD 알림 표시 설정 및 실제 Fold8 검증이 필요하다.
- 영향 파일: `docs/01_기획서.md`
- 상태: 확정

## 2026-08-18 - 자동 감지 방식

- 구분: 결정
- 내용: 충전 연결 방송만으로 장기 측정을 시작하는 구조에 의존하지 않는다. 자동 모니터링을 켠 사용자를 위한 지속 실행 구조를 우선 검토한다.
- 근거 또는 확인 방법: 최신 Android의 백그라운드 및 포그라운드 서비스 제한을 구현 직전에 다시 확인하고 Fold8에서 실측한다.
- 영향 파일: `docs/01_기획서.md`, `docs/02_구조_및_파일분배.md`
- 상태: 검증 필요

## 2026-08-18 - 코드 작성 보류

- 구분: 결정
- 내용: 기획서와 파일 분배만 작성하고 Android 프로젝트, Kotlin 코드, Gradle 설정은 아직 만들지 않는다.
- 근거 또는 확인 방법: 사용자 요청.
- 영향 파일: 전체
- 상태: 확정

## 2026-08-18 - Android 호환 기준과 배포 결정 보류

- 구분: 요구사항
- 내용: 최소 호환 버전은 Android 14(API 34)이며, 구현 시점의 최신 안정 Android 버전까지 호환을 목표로 한다. Play Store 배포 여부는 추후 결정한다.
- 근거 또는 확인 방법: 사용자 확인. 실제 구현 전 최신 Android 및 Play 정책은 다시 확인한다.
- 영향 파일: `docs/01_기획서.md`
- 상태: 확정

## 2026-08-18 - 대기 알림과 디자인 방향

- 구분: 결정
- 내용: 대기 알림 문구는 `충전 모니터`로 한다. 배터리 윤곽선과 번개 아이콘을 중심으로, 넓은 여백과 단일 강조색을 사용하는 현대적 인터페이스를 설계한다.
- 근거 또는 확인 방법: 사용자 요구사항. 실제 색상 대비와 폴드 접힘·펼침 레이아웃은 구현 후 기기에서 검증한다.
- 영향 파일: `docs/01_기획서.md`, `docs/02_구조_및_파일분배.md`
- 상태: 확정

## 2026-08-18 - Graph Engineering 개발 절차 적용

- 구분: 결정
- 내용: 기능별로 요구사항, 설계, 구현, 실제 검증, 독립 리뷰, 수정 루프를 연결해 관리한다. 결과 증거와 미검증 항목은 누적 기록한다.
- 근거 또는 확인 방법: 사용자 요구사항 및 전역 개발 절차.
- 영향 파일: `docs/04_개발_및_검증_루프.md`, `docs/03_결정_및_변경기록.md`
- 상태: 확정

## 2026-08-20 - 선택 UI와 자동 모니터링 토글 구현

- 구분: 구현
- 내용: 원형 충전 게이지, 배터리·번개 아이콘, 현재 W, 전압·전류·배터리 퍼센트, `자동 모니터링` 토글, 상세 진단 화면을 Compose UI로 구현했다. 토글 OFF는 감시 서비스와 알림을 함께 중지한다.
- 근거 또는 확인 방법: 선택한 UI 콘셉트와 사용자 승인. 대시보드, 서비스, 배터리 API, 알림, 설정 저장을 별도 Kotlin 파일로 분리했다.
- 영향 파일: `app/src/main/java/com/chargemonitor/**`
- 상태: 구현 완료, 실기기 검증 필요

## 2026-08-20 - Android 17 빌드 구성 확정

- 구분: 결정
- 내용: Android 17(API 37) 호환을 위해 AGP 9.1.1, Gradle 9.3.1, JDK 17을 사용한다. Android 14(API 34)를 최소 지원 버전으로 유지한다.
- 근거 또는 확인 방법: Android 공식 AGP 9.1.1 호환 표와 Android 17 SDK 설정 문서 확인.
- 영향 파일: `build.gradle.kts`, `app/build.gradle.kts`, `gradle/wrapper/gradle-wrapper.properties`
- 상태: 확정

## 2026-08-20 - 빌드 및 정적 검증

- 구분: 검증
- 내용: `test assembleDebug lintDebug`가 성공했다. 디버그 APK는 v2 서명과 Android Debug 인증서를 확인했다.
- 근거 또는 확인 방법: Gradle 9.3.1 실행 결과 및 `apksigner verify --verbose --print-certs` 결과.
- 영향 파일: `app/build/outputs/apk/debug/app-debug.apk`
- 상태: 통과

## 2026-08-20 - 실기기 검증 보류

- 구분: 보류
- 내용: ADB에 연결된 기기가 없어 Fold8 설치, 충전 전압·전류 측정, 상태바 알림, AOD 알림 표시는 실행하지 못했다.
- 근거 또는 확인 방법: `adb devices -l` 결과에 연결 기기 없음.
- 영향 파일: 없음
- 상태: 검증 필요

## 2026-08-20 - 설치 파일 전달 전 독립 검토

- 구분: 변경 | 검증
- 내용: 부팅 또는 시스템 제한 상태에서 포그라운드 서비스 시작이 거부되면 예외로 종료될 수 있는 경로를 안전하게 처리했다. 그 경우 서비스는 시작되지 않고 경고 로그만 남긴다.
- 근거 또는 확인 방법: 서비스 시작 경로, 매니페스트 권한, 알림·설정 저장·부팅 수신 경로를 독립 검토했다. 변경 후 `test assembleDebug lintDebug`를 다시 실행해 통과했다.
- 영향 파일: `app/src/main/java/com/chargemonitor/service/MonitoringServiceController.kt`, `docs/03_결정_및_변경기록.md`
- 상태: 정적 검증 통과, 실기기 검증 필요

## 2026-08-20 - 비공개 GitHub 전달 결정

- 구분: 결정
- 내용: 사용자가 휴대폰에서 APK를 내려받을 수 있도록 `charge-monitor` 비공개 GitHub 저장소를 만들고, 검증된 디버그 APK를 `v0.1.0` Release 자산으로 첨부한다.
- 근거 또는 확인 방법: 사용자 승인. 소스와 문서는 Git으로 관리하며, 빌드 산출물은 Release 자산으로만 전달한다.
- 영향 파일: `.gitignore`, GitHub 비공개 저장소, GitHub Release
- 상태: 완료 (`fullmetalsonic/charge-monitor` 비공개 저장소, `v0.1.0` Release)

## 2026-08-20 - Fold8 실기기 피드백 반영

- 구분: 변경
- 내용: 배터리 API가 `FULL` 상태이고 전류가 거의 0 또는 음수인 만충 상황을 `충전 완료`로 별도 표시한다. 지속 알림은 상태별 문구와 잠금화면 공개 본문을 사용한다. 대시보드는 시스템 안전영역을 반영해 하단 탐색 바와 겹치지 않게 조정한다.
- 근거 또는 확인 방법: Fold8 스크린샷에서 100%, 4.45V, -0.01A가 확인됐고 기존 상태 분류는 이를 `충전 대기 중`으로 처리했다. AOD 실제 표시 방식은 One UI 설정과 실기기 재검증이 필요하다.
- 영향 파일: 배터리 데이터 소스, 상태 분류, 알림, 대시보드, 단위 테스트
- 상태: 완료 (`v0.1.1` 비공개 Release), AOD 실기기 검증 필요

## 2026-08-20 - 방전 및 충전 시작 상태 표시

- 구분: 변경
- 내용: 충전기가 분리된 상태는 `방전 중`으로 표시한다. 충전 직후 전류값이 잠시 없을 때는 처음 두 번의 측정 동안 `충전 상태 확인 중`을 표시하고, 이후에도 값이 없을 때만 `전류 측정 불가`로 표시한다.
- 근거 또는 확인 방법: 지속 알림은 서비스 시작 직후 만들어지고 전류는 2초 간격으로 갱신된다. Fold8에서 잠금화면 AOD가 최초 알림만 표시하는 경우 혼동을 줄이기 위해 초기 상태를 분리한다.
- 영향 파일: 상태 모델·분류·안정화·서비스·알림·대시보드·단위 테스트
- 상태: 완료 (`v0.1.2` 비공개 Release), AOD 실기기 검증 필요

## 2026-08-20 - 방전 전력 표시

- 구분: 변경
- 내용: 음수 전류를 방전 방향으로 분류하되 전력의 크기는 양수로 계산해 `0.6W 방전 중` 형식으로 화면과 지속 알림에 표시한다.
- 근거 또는 확인 방법: Fold8 실기기 화면에서 4.38V, -0.13A가 확인되어 약 0.57W 방전 전력을 계산할 수 있다.
- 영향 파일: 전력 계산, 알림, 단위 테스트
- 상태: 완료 (`v0.1.3` 비공개 Release), 실기기 검증 필요

## 2026-08-20 - 토글 설명 정정

- 구분: 변경
- 내용: AOD에 와트 수가 직접 표시된다고 오해하지 않도록 자동 모니터링 토글 설명을 `충전·방전 상태를 알림으로 표시`로 수정한다.
- 근거 또는 확인 방법: Fold8 실기기에서 와트 수는 상태바·잠금화면 알림으로 확인되며, AOD 노출은 One UI 설정과 시스템 정책에 따라 제한된다.
- 영향 파일: 대시보드 문구
- 상태: 완료 (`v0.1.4` 비공개 Release)

## 2026-08-20 - 공개 릴리스 서명 및 최종 검토

- 구분: 변경 | 검증
- 내용: Git 제외 `.signing` 폴더에 4,096비트 RSA 릴리스 키를 생성하고 `release` 빌드에 연결했다. 공개 APK 파일명은 `ChargeMonitor-v0.1.4.apk`로 한다.
- 근거 또는 확인 방법: `test assembleRelease lint` 통과, 릴리스 APK의 패키지 정보와 서명 인증서 확인 예정. 이전 디버그 APK와 서명이 달라 기존 테스트 설치본은 제거 후 릴리스 APK를 설치해야 한다.
- 영향 파일: `.gitignore`, `app/build.gradle.kts`, `.signing/`(Git 제외), GitHub Release
- 상태: 완료 (`fullmetalsonic/charge-monitor` 공개, `v0.1.4` 릴리스 서명 APK)

## 2026-08-20 - 공개 README 예시 UI

- 구분: 변경
- 내용: 상태바와 내비게이션 바를 제외한 대시보드 예시 이미지를 공개 README에 추가하고, 기획 단계 설명을 현재 기능·다운로드 안내로 갱신한다.
- 근거 또는 확인 방법: 공개 저장소 방문자가 앱의 충전·방전 전력 표시 화면을 바로 이해할 수 있게 한다.
- 영향 파일: `README.md`, `assets/readme/dashboard-discharge.png`
- 상태: 완료

## 2026-08-20 - 만충 퍼센트 중복 표시 수정

- 구분: 버그 수정
- 내용: 상세 진단의 상태값에서 `100%% 충전됨`이 보이던 문제를 수정했다. 만충 상태는 중복 수치 없이 `충전 완료`로 표시한다.
- 근거 또는 확인 방법: 상태용 리소스는 포맷 함수 없이 그대로 출력돼 `%%`가 두 글자로 보였고, 배터리 잔량과 알림의 포맷 문자열은 각각 한 개 `%`를 출력하는 별도 경로임을 확인했다.
- 영향 파일: 대시보드, 상세 진단
- 상태: 완료 (`v0.1.10` 공개 Release)

## 2026-08-20 - 상태바 와트 표시 실험 기능

- 구분: 기능 추가
- 내용: 기존 자동 모니터링 아래에 사용자가 직접 켜고 끄는 `상태바 와트 표시 (실험 기능)` 토글을 추가한다. 토글은 자동으로 해제하지 않으며, Android 16 QPR1(API 36.1) 이상에서 시스템이 승격을 허용하면 상태바 칩에 충전·방전 와트를 짧게 표시한다.
- 호환: 지원되지 않는 Android 버전·기기·시스템 설정에서는 기존 지속 알림으로 안전하게 대체한다. 상태바 칩 노출은 삼성 시스템의 실제 승격 결과에 따라 달라진다.
- 근거 또는 확인 방법: 공식 Live Update의 승격 요청·짧은 중요 텍스트 API와 권한 조건을 적용했다. `test assembleRelease lint` 통과. Fold8 실기기에서의 칩 표시 여부는 설치 후 확인 필요.
- 영향 파일: 설정 저장소, 대시보드, 알림 팩토리, 포그라운드 서비스, 매니페스트, 언어 리소스
- 상태: 완료 (`v0.1.11` 공개 Release). Fold8 실기기에서의 상태바 칩 표시는 설치 후 확인 필요.

## 2026-08-20 - 30일 트렌드 기록

- 구분: 기능 추가
- 내용: 사용자가 직접 켜고 끄는 `트렌드 기록` 토글을 추가했다. 켜진 동안 2초 측정값을 5분 구간 평균으로 집계해 기기 내부에 보관하고, 30일보다 오래된 기록은 다음 저장 시 정리한다.
- 화면: 선택된 3번 시안을 바탕으로 최근 7일 날짜 선택, 하루 배터리 흐름, 충전·방전 구간, 충전 횟수·획득·방전·전력 피크 요약, 충전 전력 변화를 제공한다.
- 데이터: 네트워크·계정 권한 없이 앱 내부 파일에만 저장한다. 토글을 끄면 새 기록 저장만 멈추며 기존 기록은 유지한다.
- 근거 또는 확인 방법: 5분 구간 평균과 일별 요약의 단위 테스트 추가, `test assembleRelease lint` 통과. 실제 Fold8 화면 배치와 30일 누적 동작은 설치 후 확인 필요.
- 영향 파일: 트렌드 모델·저장소·분석 모듈·테스트·트렌드 화면·대시보드·화면 전환·언어 리소스
- 상태: 완료 (`v0.1.12` 공개 Release). 실제 Fold8 화면 배치와 30일 누적 동작은 설치 후 확인 필요.

## 2026-08-20 - 트렌드 날짜 이동 및 진입 순서 정리

- 구분: 버그 수정 · UI 조정
- 내용: 선택 날짜와 날짜 줄의 표시 범위를 분리했다. 과거 날짜를 눌러도 오늘이 날짜 줄에서 사라지지 않아 다시 오늘을 선택할 수 있다. 날짜 줄을 좌우로 밀면 7일 단위가 아닌 하루 단위로 부드럽게 이동하며, 미래 날짜는 오늘을 넘지 않는다.
- 화면: `트렌드 기록` 토글 바로 아래에 `트렌드 기록 ›`을 두고, 그 아래에 `상세 진단 ›`을 배치한다.
- 근거 또는 확인 방법: Fold8 실기기에서 과거 날짜 선택 후 오늘로 돌아갈 수 없고, 스와이프도 동작하지 않는 화면을 확인했다.
- 영향 파일: `TrendViewModel.kt`, `TrendScreen.kt`, `DashboardScreen.kt`
- 상태: 완료 (`v0.1.13` 공개 Release). `test assembleRelease lint` 통과. 실제 Fold8에서 하루 단위 스와이프와 오늘 복귀 동작 확인 필요.

## 2026-08-20 - 빠른 설정 와트 타일과 하루 시간축 트렌드

- 구분: 기능 추가 · 버그 수정 · UI 개선
- 내용: 빠른 설정 타일을 추가해 알림창을 내렸을 때 현재 충전·방전 와트를 확인하고, 타일을 눌러 자동 모니터링을 켜고 끌 수 있게 했다. 트렌드 그래프는 기록 개수에 맞춰 전체 폭을 늘리지 않고, 하루 00:00~24:00을 고정 시간축으로 사용해 실제 기록 시각 위치에만 표시한다.
- 화면: 배터리 흐름과 전력 변화 그래프에 `00 · 06 · 12 · 18 · 24` 기준을 표시한다. 날짜 줄은 손가락을 왼쪽으로 밀면 다음 날, 오른쪽으로 밀면 이전 날로 이동하도록 자연스러운 방향으로 수정한다.
- 데이터: 자정을 지나면 일별 요약이 새 날짜 기록만 선택하므로 그래프는 새 하루의 왼쪽 00:00부터 다시 쌓인다.
- 영향 파일: 빠른 설정 타일 서비스·매니페스트·설정 저장소·트렌드 시간축 모듈·단위 테스트·트렌드 화면
- 상태: 완료 (`v0.1.14` 공개 Release). `test assembleRelease lint` 통과. Fold8 실기기에서 00~24 고정 시간축, 실제 기록 시각 위치, 좌우 날짜 스와이프 방향을 확인했다. 빠른 설정 타일은 기기에 추가되며, 빠른 설정 편집 화면에서 원하는 첫 페이지 위치로 옮겨 사용할 수 있다.

## 2026-08-20 - 알림 중복 아이콘 제거

- 구분: 버그 수정
- 내용: 알림 우측의 중복 대형 아이콘을 만든 `setLargeIcon`을 제거했다. 알림의 왼쪽과 상태바는 Fold에서 이미 확인된 시스템 가로 충전 플러그 아이콘만 사용한다. 앱 홈 아이콘은 배터리·플러그 PNG를 유지한다.
- 근거 또는 확인 방법: 실기기 알림창에서 좌·우에 같은 배터리 아이콘이 함께 표시된 것을 확인했다.
- 영향 파일: `ChargeNotificationFactory.kt`
- 상태: 완료 (`v0.1.9` 공개 Release). 실제 Fold 알림창에서 좌측 단일 플러그 표시 확인 필요.

## 2026-08-20 - 원본 PNG 아이콘 적용

- 구분: UI 수정
- 내용: 벡터로 재해석한 앱·알림 아이콘을 제거하고, 승인된 원본 PNG를 사용한다. 홈 화면과 알림창의 큰 아이콘은 배터리·플러그 PNG, 상태바는 원본 플러그 PNG에서 추출한 투명 단색 심볼을 사용한다.
- 근거 또는 확인 방법: 벡터 구현이 사용자가 승인한 시안과 달라 `+` 또는 문자처럼 보이는 실기기 문제가 반복됐다.
- 영향 파일: 알림 팩토리, `res/drawable-nodpi`
- 상태: 완료 (`v0.1.8` 공개 Release). 실제 Fold 홈·알림창 아이콘 크기 확인 필요.

## 2026-08-20 - 앱·알림 아이콘 통일

- 구분: UI 변경
- 내용: 홈 화면에는 파란 적응형 아이콘 안에 흰색 배터리·플러그 조합을 사용하고, 알림·상태바에는 같은 플러그를 단순화한 단색 실루엣을 사용한다.
- 근거 또는 확인 방법: 상태바 아이콘은 시스템이 작은 단색으로 표시하므로 배터리 외곽선을 넣지 않아도 읽히는 플러그 형태로 분리했다.
- 영향 파일: 매니페스트, 알림 팩토리, `res/drawable*`
- 상태: 완료 (`v0.1.6` 공개 Release). 실제 기기에서 홈·알림 아이콘의 최종 크기 체감 확인은 후속 검증 항목.

## 2026-08-20 - 아이콘 형태 보정

- 구분: 버그 수정
- 내용: `v0.1.6`의 세로 플러그가 작은 화면에서 `+` 기호처럼 보인 문제를 수정한다. 홈 화면은 배터리 안의 가로 플러그·케이블 형태로, 알림은 같은 가로 플러그만 단색으로 표시한다.
- 근거 또는 확인 방법: 사용자가 제공한 홈 화면·알림창 실기기 화면에서 `+`로 보이는 현상을 확인했다.
- 영향 파일: `ic_launcher_foreground.xml`, `ic_notification_plug.xml`
- 상태: 완료 (`v0.1.7` 공개 Release). 실제 Fold 상태바에서의 체감 크기는 설치 후 확인 필요.

## 2026-08-20 - 주요 언어팩

- 구분: 변경
- 내용: 한국어, 영어, 일본어, 중국어 간체·번체, 스페인어, 프랑스어, 독일어, 포르투갈어(브라질), 러시아어, 아랍어, 힌디어의 대시보드·알림 핵심 문구를 시스템 언어 리소스로 추가한다.
- 근거 또는 확인 방법: `test assembleRelease lint` 통과. 아랍어의 실제 RTL 화면은 실기기 검증이 필요하다.
- 영향 파일: `res/values*`, 대시보드, 알림, 매니페스트
- 상태: 완료 (`v0.1.5` 공개 Release). 아랍어 RTL 실기기 화면 확인은 후속 검증 항목.

## 2026-08-20 - 실제 기기 화면 예시 교체

- 구분: 문서·자산 변경
- 내용: 실제 Fold 기기에서 촬영한 방전 중, 충전 중, 자동 모니터링 꺼짐 화면 3종을 README에 추가했다. 상태바와 하단 내비게이션 바만 제거해 앱 화면을 명확히 보이게 했다.
- 근거 또는 확인 방법: 각각 7.6W 방전, 10.3W 충전, 토글 OFF 상태가 원본 화면과 일치하는지 육안 확인했다.
- 영향 파일: `README.md`, `assets/readme/dashboard-*-device.png`
- 상태: 완료

## 2026-08-20 - 트렌드 시간 선택 커서

- 구분: 기능 추가 · UI 개선
- 내용: 배터리 흐름과 충전 전력 변화 그래프를 탭하거나 가로로 드래그하면, 터치 위치를 가장 가까운 5분 버킷으로 고정한다. 두 그래프는 선택 시간의 세로 기준선을 함께 표시한다. 선택 버킷에 기록이 있으면 시간·배터리 잔량·충전 또는 방전 전력을, 없으면 시간·`기록 없음`을 표시한다. 손을 떼어도 마지막 선택값은 유지된다.
- 근거 또는 확인 방법: 기존 저장 구조가 5분 단위 평균 버킷이므로, 기록되지 않은 시간을 추정값으로 표시하지 않는다. Fold8 실기기에서 빈 시간(`11:25`)의 `기록 없음`, 기록 시간(`20:00`)의 배터리 잔량·와트, 드래그 후 선택값 고정을 확인했다.
- 영향 파일: `TrendTimeline.kt`, `TrendTimelineTest.kt`, `TrendScreen.kt`, `res/values*`, `build.gradle.kts`
- 상태: 완료 (`v0.1.15` 공개 Release). `test assembleRelease lint` 통과. Fold8 실기기에서 빈 시간, 기록 시간, 가로 드래그와 손을 뗀 뒤 선택값 유지를 확인했다.

## 2026-08-20 - 구조·화면·다국어 최적화

- 구분: 최적화 · 버그 수정 · UI 개선 · 번역 보완
- 내용: 지원되지 않았던 상태바 와트 표시 실험 토글과 승격 알림 요청 경로를 제거했다. 일반 지속 알림과 빠른 설정 와트 타일은 유지한다. 모니터링은 충전 중 2초, 방전·대기 중에는 화면 상태에 따라 더 긴 간격을 사용하고, 알림의 실제 표시 내용이 바뀔 때만 다시 게시한다.
- 데이터: 트렌드 기록은 2초마다 전체 JSON 파일을 쓰던 방식을 메모리 집계 후 5분 구간 전환 또는 서비스 종료 시 안전 저장하는 방식으로 바꿨다. `AtomicFile`을 사용해 저장 도중 중단되어도 기존 기록이 손상될 위험을 낮췄다.
- 화면: Fold 가로 화면에서는 게이지와 설정·이동 영역을 좌우로 나눈다. 날짜 스와이프 방향을 자연스럽게 바로잡고, 과거 날짜에서 `오늘` 버튼으로 즉시 복귀할 수 있게 했다. 전력 그래프를 조작해도 선택 시간 카드가 바로 아래에 표시된다.
- 언어: 기존 주요 11개 언어에 새 안내 문구와 상세 진단 항목 번역을 보완했다.
- 검증: `lintDebug`, `assembleRelease` 통과. JVM 테스트 21개는 직접 JUnit 실행으로 통과했다. 한글 경로에서는 Gradle 테스트 워커가 클래스를 찾지 못하는 환경 문제를 재현해, 동일 컴파일 산출물을 직접 JUnit으로 실행해 검증했다. Fold8(Android 17)에서 서비스 재시작, 세로·가로 대시보드, 과거 날짜→`오늘` 복귀, 왼쪽 스와이프의 이전 날짜 이동, 전력 그래프 시간 선택값, 지속 알림의 단일 아이콘을 확인했다.
- 후속 수정: APK 업데이트 직후 이전의 켜짐 설정만 남고 서비스가 재시작되지 않아 토글과 본문 상태가 달라질 수 있음을 Fold8에서 발견했다. 대시보드가 열릴 때 켜짐 설정이면 서비스를 다시 시작하도록 보완했다.
- 영향 파일: 모니터링 서비스·알림·설정·트렌드 저장소·대시보드·트렌드 화면·도메인 정책·언어 리소스·테스트·README
- 상태: 완료 (`v0.1.16` 공개 Release 완료). APK 서명 인증서 SHA-256은 `229b6020bc74d06279d4ead5f8a00c119f3a599304a156bd392aeb4333410630`이며, 배포 APK SHA-256은 `F48D892ED7DFD6F98715F4DC1B5AC6DD1363FD5259E2D9149269555FC5FAEBA7`이다.

## 2026-08-20 - 정밀 트렌드 1분 평균 옵션

- 구분: 기능 추가 · UI 개선 · 번역 보완
- 내용: 트렌드 기록 행의 설명 영역을 누르면 기록 설정 시트를 열 수 있다. 사용자는 `표준 · 5분 평균` 또는 `정밀 · 1분 평균`을 선택하며, 선택한 주기는 다음 기록부터 적용된다. 두 모드 모두 최근 30일을 기기에 보관한다.
- 화면: 기록 켜기·끄기 스위치는 그대로 유지하고, 주기 선택은 별도의 설정 시트로 분리해 대시보드가 복잡해지지 않도록 했다. 대시보드 설명, 빈 그래프 안내, 전력 평균 안내, 시간 터치 커서는 선택 주기에 맞춰 각각 5분 또는 1분 기준으로 표시한다.
- 데이터: 주기를 바꾸어도 과거 기록은 삭제하지 않는다. 1분 기록을 5분 표시로 바꾸면 저장된 1분 원본을 5개씩 평균 내어 표시한다. 반대로 5분 기록을 1분 표시로 바꾸면 없는 중간 값을 만들지 않고, 과거 구간은 5분 기록으로 유지하며 새 구간만 1분 기록으로 표시한다. 새 버킷은 시간순으로 삽입·집계해 기록 순서가 뒤바뀌지 않도록 한다.
- 언어: 기존 주요 11개 언어에 설정 시트와 1분·5분 안내 문구를 추가한다.
- 영향 파일: 설정 저장소·트렌드 저장소·시간축 도메인·대시보드 제어·트렌드 화면·언어 리소스·단위 테스트·README
- 검증: `lintDebug`, `assembleRelease` 통과. JVM 테스트 27개에서 1분 원본 5개의 5분 평균 변환, 과거 5분 기록과 새 1분 기록의 혼합 표시, 주기 변경 뒤 커서 선택을 검증했다. Fold8(Android 17)에서 설정 시트, 1분 선택, 앱 재시작 뒤 선택 유지, 기본 5분 복귀를 확인했다.
- 상태: 완료 (`v0.1.17` 공개 Release 완료). APK 서명 인증서 SHA-256은 `229b6020bc74d06279d4ead5f8a00c119f3a599304a156bd392aeb4333410630`이며, 배포 APK SHA-256은 `D6E88F527FD48EDCC95D8232944441BC59490B54D5154019319CD698D442268E`이다.

## 2026-08-20 - 트렌드 기록 설정 진입성 및 README 갱신

- 구분: UI 개선 · 문서·자산 변경
- 내용: `트렌드 기록` 행에서 오른쪽 스위치는 기록 켜기·끄기만 담당한다. 왼쪽 설명 영역에는 현재 기록 주기와 강조된 `트렌드 기록 설정 ›`을 표시해, 누르면 기록 주기 설정 시트가 열린다는 점을 명확히 했다. 그래프 이동 링크와 상세 진단 링크의 역할은 그대로 유지한다.
- 문서: README 첫 설명을 지속 알림·빠른 설정 타일·30일 트렌드 기록까지 반영해 갱신하고, 기록 설정과 정밀 1분 평균 선택 화면을 추가했다. 공개 이미지에서는 상태바·통신사·시각과 하단 내비게이션 바를 잘라 개인 기기 정보가 보이지 않게 했다.
- 검증: 설정 진입 문구, 스위치 동작 분리, 실제 Fold 화면의 정보 제거 범위를 육안 확인한다. 문서 링크·이미지 경로와 변경 파일의 정적 검사를 수행한다.
- 영향 파일: `DashboardControls.kt`, `README.md`, `assets/readme/trend-recording-*-device.png`
- 상태: 완료. Fold8(Android 17)에서 설정 진입 문구·스위치 분리·표준 5분 복귀를 확인했고, README 공개 이미지는 상태바와 하단 내비게이션 바를 제외한 실제 화면으로 교체했다. `assembleRelease`, `lintDebug` 통과.

## 2026-08-21 - 잠금 상태 전원 연결·분리 기록 보정

- 구분: 버그 수정 · 실기기 검증
- 내용: 모니터링 서비스가 전원 연결·분리 브로드캐스트를 받으면 다음 측정을 즉시 수행하고, 전원 연결 직후에는 배터리 상태가 확정될 때까지 `충전 상태 확인 중`으로 표시한다. 이 짧은 확인 구간은 트렌드 기록에서 제외해 잠금 상태에서 충전 연결을 방전으로 남기지 않도록 했다.
- 분리 보정: 무선 충전기를 뗀 뒤 시스템의 `CURRENT_NOW` 값이 잠시 양수로 남을 수 있다. 이때 앱은 `방전 중` 상태만 표시하며, 음수 전류가 확인되기 전까지 이전 충전 와트나 반대 방향 와트를 표시·기록하지 않는다. 전원 연결·분리 이벤트마다 전력 평활화 창도 비워 이전 상태의 수치가 다음 상태에 섞이지 않게 했다.
- 실기기 검증: Fold8(Android 17) 잠금 상태에서 무선 충전 연결 후 `충전 중` 표시를 확인했다. 분리 직후 시스템은 `Wireless powered: false`, `status: 3`, `current now: +506515`를 보고했지만 앱은 숫자 없이 `방전 중`만 표시했다. 이후 `current now: -1128100`이 확인된 뒤 `5.6W 방전 중`으로 전환됐다. 이어 USB 유선 연결에서도 `USB powered: true`, `status: 2` 뒤 `충전 중` 전환을 확인했고, 분리 뒤 `USB powered: false`, `status: 3`, 음수 전류에서 정상 방전 와트 표시를 확인했다.
- 검증: `assembleRelease`, `lintDebug` 통과. 도메인 단위 테스트 34개는 직접 JUnit 실행으로 통과했다. Gradle 테스트 워커는 한글 경로 환경에서 모든 테스트 클래스를 실행하지 못하는 기존 환경 제약이므로, 테스트 결과는 직접 JUnit 실행으로 확인했다.
- 영향 파일: 전원 연결 관찰자·연결 확인 도메인·상태별 와트 필터·전력 평활화·모니터링 서비스·트렌드 저장소·단위 테스트
- 상태: 완료 (`v0.1.18` 공개 Release). APK 버전은 `versionCode 19` / `versionName 0.1.18`이며, 서명 인증서 SHA-256은 `229b6020bc74d06279d4ead5f8a00c119f3a599304a156bd392aeb4333410630`, 배포 APK SHA-256은 `11DADB88750EAAC16C7890255CD92DDAA4A963C6D84F11FD38882685AA0BACE8`이다. Fold8에는 같은 서명 APK를 설치해 서비스 실행까지 확인했다.

## 2026-08-21 - UI 설계 기준선 문서화

- 구분: 설계 기준 · 문서화
- 내용: 메인 대시보드, 상세 진단, 트렌드 기록, 트렌드 기록 설정 시트, 외부 알림의 역할·화면 순서·색상·여백·그래프·정보 밀도 규칙을 하나의 기준서로 고정했다. 트렌드 화면은 00~24시 고정 시간축의 막대그래프라는 현재 원칙을 명시하고, 향후 충전·방전 피크의 시각·배터리 잔량 기능은 메인이 아닌 트렌드의 `오늘의 흐름` 아래에만 두도록 정했다.
- 근거 또는 확인 방법: `DashboardScreen`, `DashboardControls`, `DiagnosticScreen`, `TrendScreen`, `TrendRecordingSettingsSheet`, 색상 토큰과 기존 Fold8 검증 기록을 대조했다. 이번 단계에서는 앱 UI 코드와 동작을 변경하지 않았다.
- 영향 파일: `docs/05_UI_설계_기준서.md`, `docs/03_결정_및_변경기록.md`
- 상태: 완료. 다음 UI 기능 구현 전 이 기준서를 설계·리뷰 체크리스트로 사용한다.

## 2026-08-21 - 트렌드 그래프 순서 및 방향별 피크 기록

- 구분: UI 개선 · 기능 추가 · 번역 보완
- 내용: 트렌드 화면의 정보 순서를 `배터리 흐름 → 충전 전력 변화 → 오늘의 흐름 → 충전 피크·방전 피크`로 정리했다. 두 그래프는 기존처럼 00~24시 고정 시간축의 막대그래프와 시간 커서를 유지한다. `오늘의 흐름` 아래에는 각 방향에서 기록된 평균 전력 중 가장 높은 값, 그 기록 시각, 당시 배터리 잔량을 구분선 기반의 두 행으로 표시한다.
- 데이터: 피크는 원시 순간값이 아니라 선택된 기록 주기의 저장 평균값 중 최대값이다. 방향별 기록에 전력값이 하나도 없으면 추정하지 않고 `기록 없음 / —`로 표시한다.
- 언어: 기존 지원 11개 언어에 충전 피크·방전 피크·시각/잔량·기록 없음 문구를 추가했다.
- 영향 파일: `TrendPeak`, `DailyTrendSummary`, `BuildDailyTrendSummary`, `TrendScreen`, 트렌드 문자열 리소스, 도메인 단위 테스트, `docs/05_UI_설계_기준서.md`
- 검증: `assembleRelease`, `lintDebug` 통과. 직접 JUnit 실행으로 전체 35개 테스트를 통과했고, 충전·방전 방향별 최대값과 전력값 없는 기록의 제외를 검증했다. 지원 언어 11개 트렌드 리소스에 새 키 4종이 모두 있는 것도 정적 검사했다. 한글 경로에서 Gradle 테스트 워커가 클래스를 찾지 못하는 기존 환경 제약 때문에, 동일 컴파일 산출물을 직접 JUnit으로 실행했다.
- 실기기 검증: Fold8(SM-F971N) 유선 ADB에서 같은 서명 APK를 덮어 설치하고 트렌드 화면을 열었다. 실제 저장 데이터로 `배터리 흐름 → 충전 전력 변화 → 오늘의 흐름 → 충전 피크 → 방전 피크` 순서가 표시됐으며, 충전 피크 `10.7W / 21:40 / 59%`, 방전 피크 `4.9W / 19:10 / 60%`가 각각 방향별로 확인됐다.
- 상태: 완료. `v0.1.19` 공개 릴리스에서 이 변경을 포함하며, APK와 GitHub Release·Fold8 설치본의 버전·해시 일치는 릴리스 검증에서 기록한다.

## 2026-08-21 - README 최신 트렌드 설명 및 공개용 실기기 이미지

- 구분: 문서·자산 변경
- 내용: README 첫 설명을 현재 기능 기준으로 갱신하고, `v0.1.19`의 트렌드 기록 순서와 충전·방전 피크 정보를 주요 기능 목록에 반영했다. 트렌드 화면의 상단 그래프와 하단 피크 정보를 각각 실제 Fold8 화면 이미지로 추가했다.
- 개인정보 처리: 원본 화면에서 상태바 영역과 하단 내비게이션 바를 정확히 잘라 통신사, 시각, 상태 아이콘, 배터리 표시가 공개 자산에 남지 않도록 했다. 앱 화면의 그래프·수치·날짜 선택 줄은 수정하거나 생성하지 않은 실제 기록 그대로다.
- 영향 파일: `README.md`, `assets/readme/trend-flow-v0119-device.png`, `assets/readme/trend-peaks-v0119-device.png`, `docs/03_결정_및_변경기록.md`
- 검증: 두 PNG를 육안 확인해 개인정보 영역이 없고, 각각 `배터리 흐름 → 충전 전력 변화`, `오늘의 흐름 → 충전 피크 → 방전 피크`를 읽을 수 있음을 확인한다.
- 상태: 완료. 앱 코드와 APK 버전은 변경하지 않는다.

## 2026-08-21 - 공개 문서 이중 언어화 및 저장소 검색 정보

- 구분: 문서 · 공개 정보 변경
- 내용: README의 핵심 설명·기능·캡션·이미지 대체 텍스트·빌드 정보를 한국어와 영어로 함께 제공한다. 각 한국어 문서에는 같은 번호의 영어 전문 문서 링크를 추가하고, `docs/en/`에 기획서·구조·결정 및 변경 기록·개발 및 검증 루프·UI 설계 기준서의 영어판을 만든다.
- 검색 정보: GitHub 저장소 설명에 Android 14 이상 배터리 충전·방전 전력 모니터, 지속 알림, 빠른 설정 타일, 30일 트렌드 기록을 명시하고 관련 주제를 등록한다. 이 정보와 영어 문서는 GitHub 저장소 검색 및 외부 검색 엔진이 앱의 목적을 해석하는 데 도움이 되지만, 검색 노출 순위나 반영 시점은 보장하지 않는다.
- 개인정보 처리: 기존 공개 이미지에는 상태바·통신사·시각·내비게이션 바가 없음을 다시 확인한다. 새 문서에는 개인 기기 식별 정보나 계정 정보를 추가하지 않는다.
- 영향 파일: `README.md`, `docs/01_기획서.md`~`docs/05_UI_설계_기준서.md`, `docs/en/*`, GitHub 저장소 설명·Topics
- 검증: Markdown 링크와 공개 이미지 경로를 정적으로 검사하고, 병합 뒤 GitHub 공개 페이지에서 README·영어 문서·저장소 설명·Topics를 다시 확인한다.
- 상태: 완료. 앱 코드·버전·APK 자산은 변경하지 않는다.

## 2026-08-21 - README 최신 Fold8 실기기 화면 교체

- 구분: 문서 · 자산 변경
- 내용: README의 예전 대시보드 3장을 제거하고, Fold8에 설치된 `v0.1.19`에서 새로 캡처한 최신 대시보드·배터리 흐름·전력 변화와 피크 기록 화면 3장으로 교체한다. 첫 화면에서 현재의 자동 모니터링·트렌드 기록 설정·00~24시 그래프·방향별 피크 기능을 함께 보여 주어 공개 APK와 README의 화면 차이로 인한 혼동을 줄인다.
- 개인정보 처리: 각 원본 캡처에서 상단 상태바와 하단 내비게이션 바 영역만 정확히 제외한다. 통신사, 시각, 시스템 상태 아이콘, 배터리 표시, 내비게이션 버튼은 공개 이미지에 포함하지 않는다. 앱 화면의 전력값·그래프·문구는 수정하지 않는다.
- 영향 파일: `README.md`, `assets/readme/dashboard-v0119-current-device.png`, `assets/readme/trend-flow-v0119-current-device.png`, `assets/readme/trend-peaks-v0119-current-device.png`; 이전 공개 대시보드 이미지 3개 삭제
- 검증: ADB로 연결된 Fold8에서 `versionName 0.1.19`를 확인하고 세 화면을 새로 캡처했다. 공개용 PNG를 육안 확인해 시스템 정보가 없고 대시보드·배터리 흐름·전력 변화·피크 정보가 읽히는지 확인한다. README 이미지 경로와 공개 GitHub 페이지는 병합 뒤 다시 확인한다.
- 상태: 완료. 앱 코드·버전·APK 자산은 변경하지 않으며, 최신 Fold8 실기기 캡처만 README 공개 자산으로 갱신한다.
